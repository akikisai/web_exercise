<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title>確認</title>
    <link rel="stylesheet" type="text/css" href="style.css">
  </head>
  <body>
    <h1>確認</h1>
    <p>
      <%
	var user=session.user;
	%>
      id:<%= user.id %>
      <%= user.name %>様
      <%= user.amount %>円
    </p>
    <%
      var keys = Object.keys(chumon);
      keys.sort();
      %>
    <ul>
      <%
	var total=0;
	for(var key of keys){
	var item = application.itemmap[key];
	if(chumon[key]>0){
	%>
      <li>
	商品番号<%= item.no %>
	商品名<%= item.name %>
	<%= item.price %>円
	<%= chumon[key] %>
	<% total += item.price*chumon[key]; %>
      </li>
      <% }%>	
      <% }%>	
    </ul>
    <hr>
    <p>合計
      <%= total %>円
      <% session.total=total; %>
    </p>
    <form action="logout.ejs" method="post">
      <p><input type="submit"></p>
    </form>
    問い合わせ:19ec043@ms.dendai.ac.jp
  </body>
</html>
